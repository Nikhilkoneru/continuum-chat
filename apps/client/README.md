# Github Personal Assistant client

Expo app targeting web and Android first.

## Environment

Set `EXPO_PUBLIC_API_URL` to the backend origin, for example:

```bash
EXPO_PUBLIC_API_URL=http://localhost:4000
```
